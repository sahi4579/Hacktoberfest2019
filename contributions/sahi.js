const name = 'Sahitya Ambati';
const message = 'I am currently working as a web developer.'

const info = (name, message) => {
    console.log(`Hello.  My name is ${name}.  ${message}`);
};

info(name, message);
