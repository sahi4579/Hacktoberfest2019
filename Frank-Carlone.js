const name = 'Frank Carlone';
const message = 'I am currently studying to become a software developer. My goal is to have a programming career and build some interesting applications.'

const contribution = (name, message) => {
  console.log(`Hello.  My name is ${name}.  ${message}`);
};

contribution(name, message);
